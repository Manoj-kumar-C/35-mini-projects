import 'package:flutter/material.dart';

class ImageSliderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Image Sliders'),
      ),
      body: Center(
        child: Text('This is the Image Slider Page'),
      ),
    );
  }
}
